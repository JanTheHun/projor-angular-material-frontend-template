{
	"map": {
		"entities": "entities",
		"basic": "basic"
	},
	"filename": "src/app/components/main-drawer/main-drawer.component.html"
}
---
<ng-container *ngFor="let table of tables">
  <button mat-raised-button class="navBtn" [matBadge]="table.counter" [matBadgeHidden]="table.counter === 0" (click)="onNavigate(table)">\{{table.table}}</button>
</ng-container>